syntax = "proto3";
package YASE.DEF;

import "texture.proto";
import "shader.proto";
import "base.proto";
import "skybox.proto";


message Environment {

    string name = 1;
    string root = 2;

	string active_scene = 3;
}

message RecentEnvironment {
	repeated Environment recent_env = 1;
}
